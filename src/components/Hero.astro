---
// Hero.astro - Fondo geométrico separado del header
---
<section class="relative min-h-screen flex items-center justify-center font-sans overflow-hidden">
	<!-- Fondo geométrico empieza más abajo (por ejemplo: 5rem) -->
	<div class="absolute inset-x-0 top-[5rem] bottom-0 z-0">
	  <div class="absolute w-full h-full bg-black clip-bottom-left"></div>
	  <div class="absolute w-full h-full bg-black clip-top-right"></div>
	</div>
  
	<!-- Contenido perfectamente centrado -->
	<div class="relative z-10 text-center px-6">
	  <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold uppercase leading-tight text-black tracking-wide">
		TUS SERVICIOS DE SOFTWARE<br />
		<span class="block">TODOTERRRENO CON LAS MUJERES</span>
	  </h1>
  
	  <div class="flex flex-col sm:flex-row justify-center items-center gap-4 mt-10">
		<a href="/about" class="px-6 py-3 bg-black text-white text-sm tracking-wider uppercase border-2 border-black hover:bg-white hover:text-black transition-all">
		  Conócenos
		</a>
		<a href="/projects" class="px-6 py-3 bg-black text-white text-sm tracking-wider uppercase border-2 border-black hover:bg-white hover:text-black transition-all">
		  Ver proyectos
		</a>
	  </div>
	</div>
  </section>

<style>
  /* Triángulo negro desde abajo a la izquierda */
  .clip-bottom-left {
    clip-path: polygon(0% 100%, 0% 0%, 50% 100%);
  }

  /* Triángulo negro desde arriba a la derecha */
  .clip-top-right {
    clip-path: polygon(100% 0%, 100% 100%, 50% 0%);
  }

  section {
    font-family: 'Orbitron', 'Coda', 'Arial Black', sans-serif;
    letter-spacing: 0.01em;
  }
</style>
